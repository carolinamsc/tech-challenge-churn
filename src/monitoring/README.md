# Monitoring

Este pacote contém rotinas executáveis de monitoramento da distribuição de entrada. O detector de drift atual usa Population Stability Index (PSI) e gera um baseline versionado para comparação com lotes futuros.

```bash
python -m src.monitoring.drift
```
